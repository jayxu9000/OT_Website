services:
- type: web
  name: OT_Website
  env: static
  buildCommand: npm run build
  staticPublishPath: ./dist
  routes:
    - type: rewrite
      source: /** # Matches all paths
      destination: /index.html # Redirects to the index.html
